# API
fastapi==0.128.8
uvicorn[standard]==0.39.0

# Supabase JWT verification. The [crypto] extra pulls in `cryptography`,
# which is required for ES256 -- base PyJWT only handles HMAC algorithms.
pyjwt[crypto]==2.13.0

# Database + config, shared with scripts/
psycopg2-binary==2.9.12
python-dotenv==1.2.1
